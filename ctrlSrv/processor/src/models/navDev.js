class NavDev { 
    constructor(mac, onboardingDate, lastConnected) {
        this.mac = mac
        this.onboardingDate = onboardingDate
        this.lastConnected = lastConnected
    }
}

export { NavDev }