# Control Server full dockerization

This docker compose file can be used as is to build and run all the required services.


## Build

docker-compose build

## Tear down

docker-compose down

## Run

docker-compose up